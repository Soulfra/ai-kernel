import { SingleLinkage } from ".";
export declare function epsilonSearch(leaves: Set<number>, clusterTree: SingleLinkage, clusterSelectionEpsilon: number, allowSingleCluster: boolean): Set<number>;
