{
  "dependencies": {
    "express": "^4.18.2",
    "glob": "^7.2.3",
    "js-yaml": "^4.1.0",
    "multer": "^1.4.5-lts.1"
  },
  "scripts": {
    "test": "npm install --prefix kernel-slate && npm --prefix kernel-slate test"
  }
}
